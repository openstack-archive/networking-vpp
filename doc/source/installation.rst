============
Installation
============

At the command line::

    $ pip install networking-vpp

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv networking-vpp
    $ pip install networking-vpp
